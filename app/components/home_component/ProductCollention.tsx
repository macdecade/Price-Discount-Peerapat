import clsxm from "@/app/utils/clsxm";

const ProductCollection = () => {
  return (
    <div
      className={clsxm(
        "w-full h-full grid grid gap-4",
        "desktop:col-span-4",
        "tablet col-span-2",
        "mobile:col-span-1"
      )}
    >
        
    </div>
  );
};
